# Blank Project (GitHub Pages)

This project has been reset to a blank starter state.

## Current behavior

- `login.html`: password entry page
- `index.html`: blank protected page
- `assets/js/auth.js`: password + session logic
- `.github/workflows/deploy.yml`: GitHub Pages deployment from `main`

## Access password

- `gobaby go`

## Run locally

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080/login.html`.
